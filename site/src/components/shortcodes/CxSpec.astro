---
/**
 * A component wrapper for specification content that enables the rehype plugin
 * to apply custom formatting (@ variants, % numbers, numbered headings, etc).
 */
---

<div class="cx-spec">
  <slot />
</div>
