---
/**
 * A component wrapper for props content that enables the rehype plugin
 * to format h4 elements by wrapping content after ":" with <span class="type">.
 */
---

<div class="cx-prop">
  <slot />
</div>
