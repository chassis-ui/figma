# Specs

Technical specifications and design system values that define the visual and structural properties of mobile navigation bottom components. These specifications ensure consistent implementation across design tools and development environments.

**Legend:**

- Brackets ([ ]) denote a component variant placeholder.
- Full emphasis indicates a value specific to that variant.
- Dimmed indicates a value unchanged from the default variant.

## Bottom Navigation Component

Specifications for mobile navigation bottom container elements including measurements, colors, typography, and platform-specific styling attributes. These values define the visual presentation and structural properties for mobile bottom navigation implementations.

### Design Tokens

Standardized design values used in mobile navigation bottom components. Includes specifications for each type variant, platform-specific styling, and context-dependent presentation rules for consistent appearance and behavior across all mobile bottom navigation implementations.

## Nav Tab Bar Component

Specifications for mobile navigation tab elements including measurements, colors, typography, and tab interaction styling attributes. These values define the tab presentation and section navigation properties for mobile bottom navigation tab implementations.

### Design Tokens

Standardized design values used in mobile navigation tab components. Includes specifications for each type variant, tab positioning configurations, interaction states, and visual hierarchy rules for consistent appearance and behavior across all mobile bottom navigation tab implementations.

## Nav Action Bar Component

Specifications for mobile navigation action elements including measurements, colors, typography, and action button styling attributes. These values define the action presentation and task completion properties for mobile bottom navigation action implementations.

### Design Tokens

Standardized design values used in mobile navigation action components. Includes specifications for each type variant, button arrangement configurations, interaction states, and functional hierarchy rules for consistent appearance and behavior across all mobile bottom navigation action implementations.

## Nav Progress Bar Component

Specifications for mobile navigation progress elements including measurements, colors, typography, and progress indication styling attributes. These values define the progress presentation and process communication properties for mobile bottom navigation progress implementations.

### Design Tokens

Standardized design values used in mobile navigation progress components. Includes specifications for each type variant, progress display configurations, information hierarchy rules, and process state styling for consistent appearance and behavior across all mobile bottom navigation progress implementations.
